{
  "name": "juventud-cba",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run db:setup && node src/index.js",
    "dev:only": "node src/index.js",
    "db:setup": "prisma generate && prisma db push && node prisma/seed.js",
    "db:generate": "prisma generate",
    "db:push": "prisma db push",
    "db:seed": "node prisma/seed.js"
  },
  "dependencies": {
    "@prisma/client": "^6.0.0",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "helmet": "^8.3.0",
    "jsonwebtoken": "^9.0.2",
    "multer": "^1.4.5-lts.1",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "prisma": "^6.0.0"
  }
}
